package oop;

//I means that the class is an interface - it is the naming convention followed. 
public interface IRate {

	//in the interface, we just create an outline and leave the implementation upto the user. 
	public void setRate();
	public void increaseRate(); 
	
}
